# basic
DEV_ROOT_PATH=${DEV_ROOT_PATH}
BASE_IP=${BASE_IP}
DOCKER_ROOT_PATH=${DEV_ROOT_PATH}/dockers

## Port
PLATFORM_PORT_START=${PLATFORM_PORT_START}
PLATFORM_PORT_END=${PLATFORM_PORT_END}

## Platform Common Ports
PLATFORM_SSH_PORT=${PLATFORM_SSH_PORT}
PLATFORM_MYSQL_PORT=${PLATFORM_MYSQL_PORT}
PLATFORM_POSTGRES_PORT=${PLATFORM_POSTGRES_PORT}
PLATFORM_N8N_PORT=${PLATFORM_N8N_PORT}
PLATFORM_WORDPRESS1_PORT=${PLATFORM_WORDPRESS1_PORT}
PLATFORM_WORDPRESS2_PORT=${PLATFORM_WORDPRESS2_PORT}
PLATFORM_WORDPRESS3_PORT=${PLATFORM_WORDPRESS3_PORT}
PLATFORM_NEXTJS_BLOG1_PORT=${PLATFORM_NEXTJS_BLOG1_PORT}
PLATFORM_NEXTJS_BLOG2_PORT=${PLATFORM_NEXTJS_BLOG2_PORT}

# databases
## Mysql
MYSQL_HOST=${BASE_IP}
MYSQL_PORT=${PLATFORM_MYSQL_PORT}
MYSQL_USER=${MYSQL_USER}
MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
MYSQL_PASSWORD=${MYSQL_PASSWORD}
MYSQL_DATABASE=${MYSQL_DATABASE}
DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${BASE_IP}:${MYSQL_PORT}/${MYSQL_DATABASE}

## PostgreSQL
POSTGRES_HOST=${BASE_IP}
POSTGRES_PORT=${PLATFORM_POSTGRES_PORT}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DB}
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${BASE_IP}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

## ssh
SSH_ROOT_PASSWORD=${SSH_ROOT_PASSWORD}

## NAS
NAS_ROOT_LOCAL=/volume1
NAS_ROOT_DOCKER=/nas

## Current Directory
CURRENT_DIR_LOCAL=./
CURRENT_DIR_DOCKER=/exposed

## bashrc, crontab
BASHRC_LOCAL=./configs/.bashrc
BASHRC_DOCKER=/root/.bashrc
CRONTAB_LOCAL=./configs/crontab
CRONTAB_DOCKER=/etc/cron.d/app-crontab

## IDE Server Paths (for VSCode and Cursor persistence)
NAS_CURSOR_SERVER_LOCAL=./configs/ide/.cursor-server
NAS_VSCODE_SERVER_LOCAL=./configs/ide/.vscode-server
NAS_CURSOR_SERVER_DOCKER=/root/.cursor-server
NAS_VSCODE_SERVER_DOCKER=/root/.vscode-server

## .claude
CLAUDE_SETTING_LOCAL=./claude
CLAUDE_SETTING_DOCKER=/root/.claude

## CHROME
### 실행 파일 경로 (Docker global installation - Playwright 1.40.0)
CHROME_EXECUTABLE_FOLDER_LOCAL=./configs/exe/chrome
CHROME_EXECUTABLE_FOLDER_DOCKER=/opt/google/chrome
CHROME_USERDATA_PATH_LOCAL=./configs/google-chrome
CHROME_USERDATA_PATH_DOCKER=/root/.config/google-chrome
CHROME_FONTS_PATH_LOCAL=./configs/fonts
CHROME_FONTS_PATH_DOCKER=/usr/share/fonts

## n8n
N8N_DATA_LOCAL=./configs/n8n_data
N8N_DATA_DOCKER=/root/.n8n
N8N_PORT=${PLATFORM_N8N_PORT}
N8N_HOST=0.0.0.0
N8N_PROTOCOL=http
N8N_SECURE_COOKIE=false
NODE_FUNCTION_ALLOW_EXTERNAL=axios,jnu-abc,jnu-doc,jnu-cloud
N8N_RUNNERS_MAX_OLD_SPACE_SIZE=2048
N8N_RUNNERS_MAX_PAYLOAD=1024
N8N_RUNNERS_ENABLED=false
N8N_RUNNERS_MODE=internal
N8N_RUNNERS_PATH=/tmp/n8n-runners
DB_SQLITE_POOL_SIZE=2
N8N_BLOCK_ENV_ACCESS_IN_NODE=false
N8N_GIT_NODE_DISABLE_BARE_REPOS=true
N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true